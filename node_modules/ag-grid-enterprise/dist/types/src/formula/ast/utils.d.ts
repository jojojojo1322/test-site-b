interface FormulaOperand {
    type: 'operand';
    value: string | number | boolean | Cell;
}
export interface FormulaOperation {
    type: 'operation';
    operation: string;
    operands: FormulaNode[];
}
export type FormulaNode = FormulaOperand | FormulaOperation;
export declare class FormulaError extends Error {
    readonly type: '#REF!' | '#NAME?' | '#CIRCREF!' | '#PARSE!' | '#VALUE!' | '#DIV/0!' | '#ERROR!';
    name: string;
    constructor(message: string, type?: '#REF!' | '#NAME?' | '#CIRCREF!' | '#PARSE!' | '#VALUE!' | '#DIV/0!' | '#ERROR!');
}
export declare class FormulaParseError extends FormulaError {
    readonly errorStart: number;
    readonly errorEnd: number;
    constructor(message: string, errorStart: number, errorEnd: number);
}
export type CellRef = {
    id: string;
    absolute: boolean;
};
export type Cell = {
    column: CellRef;
    row: CellRef;
    endColumn?: CellRef;
    endRow?: CellRef;
};
export {};
